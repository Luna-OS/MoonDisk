{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Tauri core default permissions only, no plugin permissions. The app's own commands (app_info, disks_list, disk_get, execute_operation, see src-tauri/src/commands/) are not plugin commands and need no permission entry of their own.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
