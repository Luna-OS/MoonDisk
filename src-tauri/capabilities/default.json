{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Nur Tauri-Core-Standardrechte, keine Plugin-Berechtigungen. Die App-eigenen Commands (app_info, disks_list, disk_get, execute_operation, siehe src-tauri/src/commands/) sind keine Plugin-Commands und brauchen daher keinen eigenen Permission-Eintrag.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
